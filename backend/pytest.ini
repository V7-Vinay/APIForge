[pytest]
testpaths = tests
markers =
    unit: deterministic unit tests with no external services
    integration: API/database/Redis integration tests requiring project services
    security: authentication, authorization, tenant isolation, secrets, and SSRF tests
    e2e: end-to-end tests spanning multiple application layers
asyncio_mode = auto
